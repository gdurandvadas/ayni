[project]
name = "ayni-mutmut-baseline"
version = "0.1.0"
requires-python = ">=3.12,<3.13"

[dependency-groups]
dev = ["mutmut==2.5.1", "pytest==9.0.3"]

[tool.pytest.ini_options]
pythonpath = ["src"]
