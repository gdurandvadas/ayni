name: ayni-python

on:
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  validate:
    name: validate Python mono example
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Build Ayni CLI
        uses: ./.github/actions/build-ayni

      - name: Install pinned Mise
        uses: jdx/mise-action@v3
        with:
          version: 2026.6.14
          install: false
          cache: false

      - name: Validate Python example through managed execution
        uses: ./.github/actions/validate-example
        with:
          language: python
          fixture: examples/python/mono
          expected-roots: .
