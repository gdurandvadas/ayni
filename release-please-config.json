{
  "$schema": "https://raw.githubusercontent.com/googleapis/release-please/main/schemas/config.json",
  "include-v-in-tag": true,
  "packages": {
    ".": {
      "release-type": "simple",
      "package-name": "ayni",
      "extra-files": [
        {
          "type": "toml",
          "path": "Cargo.toml",
          "jsonpath": "$.workspace.package.version"
        },
        {
          "type": "toml",
          "path": "Cargo.lock",
          "jsonpath": "$.package[?(@.name == \"ayni-adapters-common\" || @.name == \"ayni-adapters-go\" || @.name == \"ayni-adapters-kotlin\" || @.name == \"ayni-adapters-node\" || @.name == \"ayni-adapters-python\" || @.name == \"ayni-adapters-rust\" || @.name == \"ayni-cli\" || @.name == \"ayni-core\" || @.name == \"ayni-environment\")].version"
        }
      ]
    }
  }
}
